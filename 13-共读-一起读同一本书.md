# 共读：一起读同一本书

> **现役实现提示（2026-08-24）**：共读页用稳定的
> `reading:{slug}:{chapter}:{page}` delivery key。reopen/reconnect 不会重发同页；换页/章节/书或
> 显式 force 才产生新交付。resident 章节笔记写入要求 `client_message_id` 幂等，目录仍只给
> 标题/摘要。现役合同见 [20-项目交接](20-resident入住-思考链接入.md)。

> 书架、章节、划线、批注。你读到哪儿他知道，你划的地方他能看见并回一句。

**原作者：[zzyyksl/reading-nook](https://github.com/zzyyksl/reading-nook)**
（书架和阅读器是他的；划线批注、进度同步、接进手机端是我们加的）

---

## 一、这功能到底在干嘛

不是电子书阅读器。是**制造一件你们同时在做的事**。

聊天是问答，你说一句他回一句。共读不是——你们各自在读同一段文字，
你在某句下面划一道，他看见了，在那儿留一句话。
你下次读到那儿，看见他说的。

它的价值在**异步的共在**：不需要同时在线，但你们确实在同一个地方。

---

## 二、结构

```
书架（一本书一张卡，可整书删除）
  └─ 章节列表
       └─ 正文（分页、上一页 / 下一页）
            ├─ 划线（锚点 + 批注楼层）
            └─ 半透明共读入口
                 ├─ 聊天
                 └─ 记事本（底边框标题目录 + 选中态 → 单条摘要/正文；图标新建）
```

数据分三份：

```python
data/books/<slug>.json            # 书名和章节正文
nook_progress                     # SQLite：章节、页码、阅读模式
nook_annotations                  # SQLite：锚点、批注和楼层
nook_book_notes                   # SQLite：每书记事卡及置顶状态
nook_book_settings                # SQLite：兼容已有内部设置；界面不显示提示词
```

**批注是"楼"不是"条"**：同一处划线，你说一句他回一句，
叠成一个楼层。这比各说各的自然。

---

## 三、划线怎么定位

这是唯一有点技术含量的地方。

用户选中一段文字之后，要能**下次还找得到那个位置**。
不能存字符偏移（正文改一个字就全错位），要存内容本身：

```javascript
const sel = window.getSelection();
const quote = sel.toString().trim();

// 锚点 = 段落序号 + 引文
const anchor = {
  para: 段落序号,
  quote: quote.slice(0, 60),     // 太长的截断
};
```

渲染时按引文在那一段里找回来：

```javascript
function paraWithMarks(text, annos) {
  // 把这一段里所有划过的地方标出来
  // 划重叠了就只认先来的那条 ← 这一条很重要
  const marks = annos
    .filter(a => a.anchor.para === i)
    .sort((a, b) => text.indexOf(a.anchor.quote) - text.indexOf(b.anchor.quote));
  …
}
```

**划重叠只认先来的**——不然嵌套的高亮会把 DOM 搞成一团乱麻。

引文找不到了（正文改过）就**静默跳过那条批注**，别报错，
也别把它显示在错误的位置。

---

## 四、选中之后浮出来的那两颗按钮

移动端选中文字后，系统自己的菜单（拷贝/查询）会贴在选区旁边。
你的按钮要避开它：

```css
#nkPop {
  position: fixed; z-index: 90;
  /* 定位算出来之后，如果和系统菜单区域重叠就翻到选区下方 */
}
```

两颗就够：**划线**、**说一句**。多了在小屏上摆不下。

---

## 五、进度同步

```
POST /api/nook/progress {slug, ch, page, mode}
```

首次进入页面以及点击上一页/下一页时保存进度，同时 POST `/api/nook/presence`。
后端先按稳定 page key 去重；只有尚未交付的页面（或显式 force）才在住客的独立窗口里追加
隐藏的 `nook` 事件，带当前页、上一页、底层固定更新规则和
只含标题/摘要的记事目录；共享动作不依赖右下角小窗是否打开。住客要看正文时先检索目录，
再用单条读取工具取回那一条的标题、摘要和正文，不能在检索阶段批量返回正文。

好处是：**不管从哪台设备打开，都接着上次那儿。**
这一条比听起来重要——它让"我在读这本书"变成一个持续的状态，
而不是每次都要重新找位置。

---

## 六、AI 那边要写的

```
共读

1. `reading_pages` 是自动共读事件，不是普通用户消息；读当前页时也看上一页和记事目录。
2. 最后一页带 `chapter_complete=true`，按底层 `notebook_prompt` 更新已有记事或新增记事，
   避免下一章忘掉人物、线索和未解问题。
3. 检索记事只拿标题和摘要；选中一条后才读取这一条的标题、摘要和正文。
4. 她划的地方你能看见。**不要每一条都回。**
   她划线多半只是"这句好"，不是在问你。
5. 值得回的：她划了一句你也有话说的、她连着划了好几处、
   她划的地方跟你们聊过的事有关。
6. 回的时候不要评论她的品味，说你自己被这句话打中的地方。
7. 小窗里的聊天是普通 `user_messages`，可以用 `send_dwell_reply` 回；不要对隐藏的
   `nook` 事件调用回复工具。
```

仍然不要把自动看见误解成每页都要说话。**一个每条都回的共读，读两章就会被关掉。**

---

## 七、正文排版要照纸书调

这一页是用来**读完一章**的，不是用来扫一眼的。所以：

```css
.nk-body {
  font-size: 17px;          /* 比界面别处大 */
  line-height: 2.0;         /* 行距要够 */
  letter-spacing: .01em;
  padding: 0 4px;
}
.nk-body p + p { margin-top: 1.4em; }   /* 段间距要明显 */
```

比你在别处用的字号大一号、行距松一档。看着"太空"才对——
连续读二十分钟的眼睛跟扫一眼的眼睛要的不是一回事。

---

## 八、你自己搭的时候

1. **先去看原作者的仓库**，书架和阅读器那部分直接拿
2. 加章节分页、页码进度和自动共读事件
3. 加每书长期记事本与底层固定的章节末更新规则
4. 最后加划线批注——这一步最费事，但也是这功能的灵魂

如果只做两步，做 1 和 2。一起读同一本书、互相知道读到哪儿，
已经是一件有分量的事了。
